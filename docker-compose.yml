services:
  api:
    build: ./api
    image: dkrypt
    container_name: dkrypt
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env
    environment:
      PORT: 8080
      BIND_HOST: 127.0.0.1
    volumes:
      - ipadecrypt-config:/root/.ipadecrypt
      - ipa-tmp:/data/tmp
      - ipadecrypt-state:/data/state
      - ${HOME}/.ssh/id_ed25519_ipad:/root/.ssh/id_ed25519:ro

volumes:
  ipadecrypt-config:
    name: ipadecrypt-service_ipadecrypt-config
  ipa-tmp:
    name: ipadecrypt-service_ipa-tmp
  ipadecrypt-state:
    name: ipadecrypt-service_ipadecrypt-state
